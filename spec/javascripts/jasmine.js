describe('Map rendering', function () {

  describe('GoogleMaps rendering correctly', function () {
    beforeEach(function() {
      results = ;
    });
    it ('should find map_canvass is not empty', function () {
      expect(results).toBe();
    });
  });

  describe('markers drawn on map', function () {
    beforeEach(function() {
      results = ;
    });
    it ('should have map markers doing something', function () {
      expect(results).toBe();
    });
  });

  describe('each marker is added to images_list', function () {
    beforeEach(function() {
      results = ;
    });
    it ('images_list div contains the list items', function () {
      expect(results).toBe();
    });
  });

  describe('map re-centering upon address entered', function () {
    beforeEach(function() {
      results = ;
    });
    it ('setCenter changes to match search latlng', function () {
      expect(results).toBe();
    });
  });
  describe('each clicked marker runs modal', function () {
    beforeEach(function() {
      results = ;
    });
    it ('click handle results in images_result toggling', function () {
      expect(results).toBe();
    });
  });
  describe('clicked list item runs modal', function () {
    beforeEach(function() {
      results = ;
    });
    it ('clicked list item results in images_result modal toggling', function () {
      expect(results).toBe();
    });
  });
)};
